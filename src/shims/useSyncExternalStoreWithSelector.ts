import * as selectorShim from 'use-sync-external-store/shim/with-selector'

const { useSyncExternalStoreWithSelector } = selectorShim

export { useSyncExternalStoreWithSelector }

export default {
  useSyncExternalStoreWithSelector,
}
